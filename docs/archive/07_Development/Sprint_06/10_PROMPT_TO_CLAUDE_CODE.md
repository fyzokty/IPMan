Implement **Sprint 06 — Network Configuration Preflight** for IPMan.

Read and follow:
- `.claude/CLAUDE.md`
- `.claude/ARCHITECTURE_RULES.md`
- `docs/02_Architecture/01_Technical_Architecture.md`
- `docs/02_Architecture/04_Configuration_Apply_Workflow.md`
- relevant networking ADRs
- every file in `docs/07_Development/Sprint_06/`

Inspect the existing Sprint 05 code before making changes.

Implement Sprint 06 only.

Do not implement any Windows network mutation and do not add Apply/DHCP
functionality.

After implementation:
1. build Debug,
2. build Release,
3. run all tests,
4. fix all warnings/errors,
5. complete `09_CLAUDE_COMPLETION_TEMPLATE.md`,
6. stop.

For git review:
- if uncommitted, provide `git diff --stat`;
- if already committed, provide `git show --stat HEAD`;
- if multiple commits, provide the baseline-to-HEAD stat.

Do not dump full source files or full git diffs into chat unless specifically
asked during architect review.

Do not begin Sprint 07.
